export default function ParallaxLanding() {
  return (
    <main style={{ padding: "3rem", fontFamily: "sans-serif" }}>
      <h1>🫖 Welcome to CTea Newsroom</h1>
      <p>If you can see this, the stack is fine — now isolate the component that crashes the page.</p>
    </main>
  );
}
